# AR version snapshots

Mutable copies of `ar/` created by `improve()` during the outer loop live here as
`ar_v_<random>/` directories. This folder is gitignored except this README and
`.gitkeep`.

Override location with `AR2_CACHE_DIR` in `.env`.
