"""Modal deployment: parallel rollouts and app session lifecycle.

Import submodules directly, e.g. ``from harness.cloud.runner import run_rollouts_parallel``.
Eager re-exports are avoided so tests can stub ``modal`` before importing infra images.
"""
