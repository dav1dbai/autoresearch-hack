"""Pluggable GPU compute backends (local CPU, Modal GPU, Vast SSH).

Import submodules directly, e.g. ``from harness.backends.gpu import make_gpu_backend``.
Implementations: ``local.py``, ``modal_gpu.py``, ``vast.py``.
"""
