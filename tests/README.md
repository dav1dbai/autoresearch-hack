# Integration tests only

Unit tests are colocated next to the code they exercise (`envs/test_*.py`, `harness/**/test_*.py`, etc.). This directory holds cross-package integration and smoke wrappers only.
